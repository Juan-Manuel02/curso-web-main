/*
Crea un objeto carrito que tenga un array de productos (obj: nombre y precio), array de precios y métodos para agregarProducto(nombre, precio),
 calcularTotal() y mostrarProductos().
*/
/*const carrito = {
    productos: [],
    agregarProducto(nombre, precio) {
        let producto = {
            nombre: nombre,
            precio: precio
        }
        this.productos.push(producto);
        return `Producto ${nombre} agregado al carrito.`;
    }
}
calcularTotal(){
    let total = 0;
    let productos = this.productos;
    for (let i = 0; i < productos.length; i++) {
        total += productos[i].precio;
    }
    return total;
}


carrito.agregarProducto("Cerveza", 3);
carrito.agregarProducto("Coca-Cola", 2.50);
console.log(carrito.productos);*/

/*const precios = {
    precios: [],
    añadirPrecio(precio){
        let precioProducto = {
            precio: precio
        }
        this.precios.push(precioProducto);
    }
}
precios.añadirPrecio(3);
precios.añadirPrecio(2.50);
console.log(precios.precios);*/


